# MAAFW-火影忍者手游——编译、维护与排查说明

最后更新：2026-08-15
基准环境：已实际编译成功（BUILD SUCCESSFUL in 8m50s）

本文是本项目唯一的编译说明。编译环境和命令以本文为准。

## 一、项目与编译环境

项目原始目录：
  /storage/emulated/0/火影MAA安卓脚本开发/MAAFW-Android-火影忍者手游/

实际编译工作区（proot/Linux，可执行）：
  /root/maa_build/MAAFW-Android-火影忍者手游/

Android SDK：
  /root/android-sdk

SD 卡 SDK 归档：
  android-sdk/（约 447 MB）

注意：SD 卡文件系统为 noexec，二进制程序不能直接在其中运行，因此不要直接在
/storage/emulated/0 或 /sdcard 下构建。需要恢复 SDK 时，将归档的 android-sdk/
完整复制到 /root/android-sdk。

已验证环境：
  Java：OpenJDK 17（ARM64，proot 系统内）
  platforms;android-34
  build-tools;34.0.0
  platform-tools（adb）
  cmdline-tools（sdkmanager 12.0）

build-tools/34.0.0 中的 aapt2、aapt、aidl、zipalign、dexdump 已替换为
ARM64 可执行版本。Google 官方 x86_64 版本备份位于：
  /root/android-sdk/build-tools/34.0.0-x86

项目依赖仓库使用阿里云 Maven 镜像，Gradle 发行版使用阿里云镜像。
gradle.properties 中的 android.aapt2FromMavenOverride 指向 ARM64 aapt2。

## 二、标准编译流程

1. 将最新源码同步到可执行工作区

  rm -rf /root/maa_build/MAAFW-Android-火影忍者手游/app/src/main
  cp -r "/sdcard/火影MAA安卓脚本开发/MAAFW-Android-火影忍者手游/app/src" \
    /root/maa_build/MAAFW-Android-火影忍者手游/app/

1. 设置环境并编译

  export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-arm64
  export ANDROID_HOME=/root/android-sdk
  cd /root/maa_build/MAAFW-Android-火影忍者手游
  ./gradlew :app:assembleDebug

已有完整 Gradle 缓存时，也可以离线编译并只查看末尾输出：

  ./gradlew :app:assembleDebug --offline -q 2>&1 | tail -20

若提示缺少依赖，去掉 --offline，或先执行：

  ./gradlew :app:dependencies

编译成功时会看到 BUILD SUCCESSFUL，APK 位于：
  app/build/outputs/apk/debug/app-debug.apk

1. 将 APK 同步回 SD 卡

  cp app/build/outputs/apk/debug/app-debug.apk \
    "/sdcard/火影MAA安卓脚本开发/MAAFW-Android-火影忍者手游/app/build/outputs/apk/debug/app-debug.apk"
  cp app/build/outputs/apk/debug/app-debug.apk \
    "/sdcard/Download/maafw-naruto-debug.apk"

已使用过的分发位置：
  /sdcard/Download/maafw-naruto-debug.apk
  /sdcard/安卓环境/maafw-naruto-debug.apk
  [项目]/app/build/outputs/apk/debug/app-debug.apk

## 三、Native/NDK 编译

`app/src/main/native/` 是当前维护中的 native 源码，不再仅供阅读。Gradle 已通过
`externalNativeBuild` 接入该目录下的 `CMakeLists.txt`，执行标准 APK 构建时会自动编译：

  bridge（C++）→ libbridge.so
  launcher（C）→ liblauncher.so

当前仅构建 `arm64-v8a`，C++ 标准为 C++17。标准命令不变：

  ./gradlew :app:assembleDebug

不需要预先运行 `ndk-build`，也不要再从发行版 0.1 APK 恢复旧的 `libbridge.so`。
`app/src/main/jniLibs/arm64-v8a/` 只保留 MaaFramework 及其依赖等预编译库；
`libbridge.so` 和 `liblauncher.so` 由 CMake 在构建目录中生成并由 Android Gradle Plugin
自动合并进 APK。不要在 `jniLibs` 中同时放置这两个同名文件，否则会产生重复 native
库或误打包旧产物的问题。

当前 native 配置入口：
  app/build.gradle
  app/src/main/native/CMakeLists.txt

`Android.mk` 和 `Application.mk` 可用于独立的 AIDE/ndk-build 场景，但不是标准 Gradle
构建链。若修改 native 源码，应至少重新执行一次完整 APK 构建，不要复用旧 `.so`。

MaaFramework 相关 `.so` 仍为预编译依赖，位于：
  app/src/main/jniLibs/arm64-v8a/

## 四、MaaFramework 已知红线

1. 不要注册 MaaTaskerAddContextSink 的 focus 监听。
   在 v5.12.3/v5.13.0-beta.2 中，节点事件回调曾导致引擎随机崩溃或卡死。

2. 不要使用 MaaFramework Swipe 的多途径点形式（end 数组配 duration 数组）。
   NonlinearSwipe 已改为引擎层直接注入，见 CustomActions.kt。

3. 不要在 CustomRecognition 中通过 MaaContextRunRecognitionDirect 嵌套 OCR。
   已验证会死锁；point_race_challenge 已改用原生 OCR。

4. CustomAction 阶段拿不到 image，不能在动作阶段执行 OCR。

更完整的历史分析见：滑动卡死问题修复报告.md

## 五、资源部署与测试

资源源目录：
  app/src/main/assets/resource/base/

App 启动后部署到：
  /storage/emulated/0/Android/data/com.maafw.naruto/files/resource/base

AssetResourceDeployer 使用 .maafw_version 校验资源版本。修改 assets 资源后，应提升
app/build.gradle 中的 versionCode，以强制重新部署；否则设备上可能继续使用旧资源。

安装 APK：
  adb install -r /sdcard/Download/maafw-naruto-debug.apk

若新版本安装后异常，可确认安装的是 app-debug.apk，并在必要时卸载旧版本后重装。

## 六、关键文件

app/src/main/java/com/maafw/naruto/remote/RemoteEngineServiceImpl.kt
  引擎服务、任务执行、引擎复用和后台保障；focus 监听已禁用。

app/src/main/java/com/maafw/naruto/maa/CustomActions.kt
  自定义动作，包括 NonlinearSwipe、MultiSwipeCustom、SelectLowestChallenge 等。

app/src/main/java/com/maafw/naruto/maa/CustomRecognitions.kt
  自定义识别实现。

app/src/main/java/com/maafw/naruto/maa/AssetResourceDeployer.kt
  assets 资源部署及 .maafw_version 校验。

app/src/main/java/com/maafw/naruto/service/
  前台服务和通知系统。

app/src/main/java/com/maafw/naruto/schedule/
  定时任务、Root 守护与唤醒。

app/src/main/assets/interface.json
  界面配置和选项 override。

app/src/main/assets/resource/base/pipeline/
  MaaFramework Pipeline 脚本。

## 七、日志与排查

导出日志目录：
  /storage/emulated/0/MaaFw日志/

引擎实时日志：
  /storage/emulated/0/Android/data/com.maafw.naruto/files/maa_logs/maafw.log

任务卡住时，先查看 maafw.log 末尾最后一个 Node、Action 或 Recognition 事件，
定位卡住节点，再对照本文“已知红线”排查。

## 八、版本记录

v1.1：MaaFramework v5.12.3、从当前源码自动构建 libbridge/liblauncher、
      point_race 原生 OCR、通知系统、定时任务导入/导出和引导页权限申请。
