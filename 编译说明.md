# MAAFW-Android-火影忍者手游 编译说明喵～

小夜把编译这个项目的完整步骤写下来啦，主人照着做就能编译出 APK 喵～

---

## 一、项目说明

- **项目路径**：`/storage/emulated/0/火影MAA安卓脚本开发/MAAFW-Android-火影忍者手游/`
- **编译方式**：路线 A —— 使用 AndroidIDE 内置 Gradle + 本地 Android SDK 编译（推荐）
- **目标 APK**：`app/build/outputs/apk/debug/app-debug.apk`
ndk位置
/data/ndksupport-1710240003/android-ndk-aide/ndk-build clean

/data/ndksupport-1710240003/android-ndk-aide/ndk-build -j14

> 路线 A 不需要装 Termux / 额外容器，直接在 AndroidIDE 的终端里跑命令就行喵。

---

## 二、环境准备

### 2.1 安装 AndroidIDE

在 Android 设备上安装并打开 **AndroidIDE**，它自带：

- JDK 17 / JDK 21
- Gradle 8.5
- Android SDK（默认路径 `/root/android-sdk`，或 AndroidIDE 自己配置的路径）

如果没有 AndroidIDE，也可以用 **ZeroTermux** + Ubuntu proot 环境编译（路线 B），但本说明以路线 A 为准喵。

### 2.2 确认环境变量

打开 AndroidIDE 终端，检查环境：

```bash
java -version
gradle -v
ls $ANDROID_HOME/build-tools/
```

如果 `ANDROID_HOME` 没设置，可以临时设置：

```bash
export ANDROID_HOME=/root/android-sdk
export PATH=$PATH:$ANDROID_HOME/tools:$ANDROID_HOME/platform-tools
```

---

## 三、关键配置

### 3.1 aapt2 路径问题

AndroidIDE 默认下载的 aapt2 可能会报错，所以编译时要指定本地 aapt2：

```bash
-Pandroid.aapt2FromMavenOverride=$ANDROID_HOME/build-tools/34.0.4/aapt2
```

如果你的 `build-tools` 版本不是 `34.0.4`，请改成实际存在的版本目录喵。

### 3.2 Gradle Daemon

为了避免 Gradle Daemon 常驻内存，建议加 `--no-daemon`，每次编译都是独立进程喵。

---

## 四、编译命令

### 4.1 完整编译（推荐）

在项目根目录执行：

```bash
cd /storage/emulated/0/火影MAA安卓脚本开发/MAAFW-Android-火影忍者手游
export ANDROID_HOME=/root/android-sdk
bash gradlew :app:assembleDebug --no-daemon \
  -Pandroid.aapt2FromMavenOverride=/root/android-sdk/build-tools/34.0.4/aapt2
```

### 4.2 只看最后几十行输出

```bash
cd /storage/emulated/0/火影MAA安卓脚本开发/MAAFW-Android-火影忍者手游
export ANDROID_HOME=/root/android-sdk
bash gradlew :app:assembleDebug --no-daemon \
  -Pandroid.aapt2FromMavenOverride=/root/android-sdk/build-tools/34.0.4/aapt2 \
  2>&1 | tail -80
```

### 4.3 清理后重新编译

如果中途换过 JDK / NDK / aapt2，建议先清理：

```bash
cd /storage/emulated/0/火影MAA安卓脚本开发/MAAFW-Android-火影忍者手游
bash gradlew clean --no-daemon
```

然后再执行 4.1 的命令喵。

---

## 五、编译成功标志

终端最后会出现：

```text
BUILD SUCCESSFUL in 1m 13s

33 actionable tasks: 4 executed, 29 up-to-date
> Task :app:assembleDebug
```

生成的 APK 在：

```
/storage/emulated/0/火影MAA安卓脚本开发/MAAFW-Android-火影忍者手游/app/build/outputs/apk/debug/app-debug.apk
```

---

## 六、常见报错与解决

### 6.1 aapt2 报错

```text
Could not resolve all files for configuration '...'.
> Could not find com.android.tools.build:aapt2:...
```

**解决**：加上 `-Pandroid.aapt2FromMavenOverride=.../aapt2`。

### 6.2 NDK / CMake 找不到

本项目主要用 Kotlin + Java + 少量 JNI，不需要手动配置 NDK。如果提示缺失 NDK，在 AndroidIDE 的 SDK Manager 里下载 **NDK (Side by side)** 或修改 `local.properties`：

```properties
sdk.dir=/root/android-sdk
ndk.dir=/root/android-sdk/ndk/25.2.9519653
```

### 6.3 Gradle Daemon 内存不足

```bash
bash gradlew :app:assembleDebug --no-daemon -Dorg.gradle.jvmargs=-Xmx4g \
  -Pandroid.aapt2FromMavenOverride=/root/android-sdk/build-tools/34.0.4/aapt2
```

### 6.4 编译成功但安装闪退

- 卸载旧 APK 再安装新的，避免 so 库或 SharedPreference 冲突喵。
- 确认安装的是 `app-debug.apk`，不是 `app-debug-androidTest.apk`。

---

## 七、安装 APK

### 7.1 通过 ADB 安装

```bash
adb install -r /storage/emulated/0/火影MAA安卓脚本开发/MAAFW-Android-火影忍者手游/app/build/outputs/apk/debug/app-debug.apk
```

### 7.2 手动安装

在文件管理器里找到 APK，点击安装即可喵。

---

## 八、编译参数速查

| 参数 | 作用 |
|------|------|
| `:app:assembleDebug` | 只编译 app 模块的 debug 包 |
| `--no-daemon` | 不使用 Gradle Daemon |
| `-Pandroid.aapt2FromMavenOverride=...` | 指定本地 aapt2 |
| `-Dorg.gradle.jvmargs=-Xmx4g` | 给 Gradle 分配 4G 内存 |
| `clean` | 清理构建缓存 |

---

## 九、路线 B（备用）

如果 AndroidIDE 编译不过，可以用 **ZeroTermux** + Ubuntu proot：

```bash
# 在 ZeroTermux 里进入 proot Ubuntu
proot-distro login ubuntu

# 安装 OpenJDK 和 Android SDK 后执行
cd /sdcard/火影MAA安卓脚本开发/MAAFW-Android-火影忍者手游
export ANDROID_HOME=/opt/android-sdk
bash gradlew :app:assembleDebug --no-daemon \
  -Pandroid.aapt2FromMavenOverride=/opt/android-sdk/build-tools/34.0.4/aapt2
```

路线 B 适合 AndroidIDE 环境被搞坏的情况，但配置 SDK 麻烦一点喵。

---

主人按这个说明就能自己编译啦～有问题贴完整报错给小夜喵～
