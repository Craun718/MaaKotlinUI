# 滑动卡死 / 启动崩溃 问题根因分析与修复报告

> 日期：2026-08-12
> 项目：MAAFW-Android-火影忍者手游
> 结论：**问题已修复**（滑动不卡、启动不崩），本文记录根因与后续规避清单。

---

## 一、问题现象

1. **多途径点滑动卡死**：任务跑到 `down_swipe_for_ninja_guide_find_funtion_entry`（忍界指引找功能入口）时，
   执行 `NonlinearSwipe` 生成的 `custom_swipe`（MaaFramework Swipe action，end 数组 7 段）→ **引擎卡死**（33s 无日志）→ 引擎进程退出；
2. **启动即崩溃**：部分版本引擎在 `start_up` 识别阶段就断开（比滑动卡死更早）；
3. 排查期间**多次误导**：一度以为是 libbridge.so 重编译、引擎复用、启动并行化、触摸注入导致，逐一回退验证后排除。

---

## 二、根因分析（三个独立问题，需区分）

### 根因①：MaaFramework v5.13.0-beta.2 的「多途径点 Swipe」执行卡死
- `NonlinearSwipe` 自定义动作生成 `{"action":"Swipe","end":[[x1,y1],...7点],"duration":[d1,...7个]}`（多途径点）；
- **MaaFramework v5.13.0-beta.2 执行多途径点 Swipe（end 数组 + duration 数组）会卡死引擎**（ActionTask::run_impl 后无任何日志）；
- 单点 end（线性滑动）或普通点击均正常 → 是**多途径点路径的 bug**，与 App 代码、libbridge 无关。

### 根因②：MaaTaskerAddContextSink（节点事件监听）导致引擎随机崩溃
- 为显示 focus（当前操作）注册了 `MaaTaskerAddContextSink` 收 Node.* 事件；
- **v5.13.0-beta.2 注册 ContextSink 后，节点事件回调会导致引擎随机崩溃**：
  - 有时启动即死（start_up 识别阶段断开），有时滑动处卡死；
- 证据链（3 组对比）：
  | 版本 | ContextSink | 现象 |
  |---|---|---|
  | 发行版 0.1（无） | ❌ | 正常 |
  | focus 禁用 | ❌ | 能过 start_up，卡在滑动（根因①） |
  | focus 恢复 | ✅ | 启动即死 |

### 根因③（前期误导）：libbridge.so 重编译 ABI 不兼容
- 曾用 ndk-build 重编译 libbridge.so（加 fps 统计），产物与原版（82KB，`6807aea6`）ABI/行为不同；
- **重编译的 libbridge.so 也可能触发滑动异常**（与原版行为差异）；
- 结论：**libbridge.so 必须保持原版，不要重编译**。

---

## 三、最终修复方案（方案B：引擎层直接注入，不改 maafw/libbridge）

### 1. NonlinearSwipe → 引擎层直接注入（绕开多途径点）
`app/src/main/java/com/maafw/naruto/maa/CustomActions.kt`：
- 不再调用 `MaaContextRunAction(context, "custom_swipe", ...)`（MaaFramework Swipe action）；
- 改为**引擎 Kotlin 直接注入**单指触摸序列：`InputInjector.touchDown → 沿非线性路径逐点 touchMove → touchUp`；
- 保留非线性（缓出曲线）轨迹与总时长，行为等效且稳定。

### 2. focus 监听（ContextSink）→ 禁用
- 注释 `registerFocusSink(maa, resourceBase)`（RemoteEngineServiceImpl）；
- focus 显示后续改用 **App 侧解析 `maa_logs/maafw.log`** 的安全方案（不注册任何回调、不改 maafw）。

### 3. libbridge.so → 保持原版
- 从发行版 0.1 提取原版 `libbridge.so`（md5 `6807aea6`，82200B）放回 `jniLibs/arm64-v8a/`；
- 其他 MaaFramework .so 与发行版 0.1 逐 md5 核对一致。

---

## 四、后续规避清单（重要！防止更新再踩）

| # | 规则 | 原因 |
|---|---|---|
| 1 | **不要重编译 libbridge.so**（保持原版 `6807aea6`） | 重编译产物 ABI/行为不兼容 → 滑动异常/卡死 |
| 2 | **不要注册 `MaaTaskerAddContextSink`** | v5.13.0-beta.2 节点事件回调 → 引擎随机崩溃 |
| 3 | **不要用 MaaFramework Swipe 的多途径点**（end 数组 + duration 数组） | v5.13.0-beta.2 执行多途径点 Swipe → 引擎卡死 |
| 4 | 需要 focus 显示 → **App 侧解析 maafw.log** | 零回调、零 native 改动 |
| 5 | 需要帧率显示 → **纯 Kotlin**（getFrameCount 增量 + 识别事件计数） | 不依赖 libbridge 新增 JNI |
| 6 | 引擎复用 / 后台保障（唤醒锁、userActivity、临时保活、Root 守护） | 已验证安全，可保留 |
| 7 | 升级 MaaFramework 版本前，**先验证多途径点 Swipe 与 ContextSink** | 上游 beta 版本行为不稳定 |

---

## 五、当前版本状态（v1.1，已验证）

- ✅ 滑动不卡（NonlinearSwipe 直接注入）
- ✅ 启动不崩（ContextSink 禁用）
- ✅ libbridge/MaaFramework 原版
- ✅ 引擎复用 / 后台保障 / 纯 Kotlin 帧率
- ⏳ focus 彩色显示：暂缺（待 App 侧日志解析方案）
